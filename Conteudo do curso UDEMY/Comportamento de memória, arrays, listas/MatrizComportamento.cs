/*List<string> list = new List<string>();
            {
                // Matrizes
                // revisão de conceito de Matriz;
                // em programação: a "matriz" é o nome dado a array bidimensional. 9x9 ou 3x3 por linhas e colunas //
                // estrutura homogenea/ordenada/Alocada de uma vez só em um bloco de memoria;
                // Vantagem: Acesso imediato aos elemntos pela posição;
                // Desvantagem: tamanho fixo, Dificuldade de inserção e deleção;

                // Declaração e instaciamento;

                // Acesso aos elementos / como percorrer uma matriz;

                // Propriedade Length, Rank e GetLength

                // ref:// no material de apoio

                double[,] mat = new double[2, 3];

                Console.WriteLine(mat.Length);// total de elementos

                Console.WriteLine(mat.Rank);// total de linhas

                Console.WriteLine(mat.GetLength(0));// total de linhas

                Console.WriteLine(mat.GetLength(1));// total de colunas
                




            }*/